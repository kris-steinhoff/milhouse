"""milhouse's test suite.

A package rather than loose modules so ``tests.fakes`` can be imported with a
relative import from every test, which keeps the fake and the tests that use it
obviously the same thing.
"""
