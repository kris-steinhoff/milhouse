# Add a hello command

Add a `hello` subcommand to milhouse that prints a greeting, with a test
covering it and an entry in `docs/usage.md`.

This exists as the smallest real task definition to point milhouse at, both for
the quickstart in the README and for the end-to-end check in `docs/usage.md`.
